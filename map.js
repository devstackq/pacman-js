//todo pacman, ghost ui
//add all functional, check fps
//add teleport, cookie
//ideas #12 if currPos propsX < 15 -> mod = propsX % 30, propsX-=mod else propsX += mod
//try - 2 ovject  || much more object - garbage collector,
//delete comments

// temp[0].style.transform = `translate(${obj.posX}px, ${obj.posY}px)`;
//composite layer, garbage collector
//currPos 870 % 30  != 0, if currPos  % 30 < 15, = currPos -=15 else currPos- currPos% 30 + 30
//fix bug, when turn, skip coin ?